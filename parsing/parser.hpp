#ifndef PARSER_HPP
#define PARSER_HPP

class  parser
{
public:
// constructors & destructor
  parser();
  ~parser();
  parser(const parser& parser);

// operators
  parser	&operator=(const parser& parser);

// member functions
};

#endif // PARSER_HPP
