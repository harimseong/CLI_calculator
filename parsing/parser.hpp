#ifndef PARSER_HPP
#define PARSER_HPP

namespace parsing
{

class  parser
{
public:
// constructors & destructor
  parser();
  ~parser();
  parser(const parser&);

// operators
  parser	&operator=(const parser&);

// member functions
};

}

#endif // PARSER_HPP
